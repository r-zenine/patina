//! `patina-detect-tui` — the triage UI for `patina-detect` findings,
//! bootstrapped on `tui-harness`/`tui-elm`/`tui-design`.
//!
//! Registered as a workspace member ahead of its own implementation phase
//! (roadmap Phase 3); intentionally empty until then.
